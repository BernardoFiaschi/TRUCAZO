#pragma once
#include <vector>
#include <string>
#include <map>
#include "Carta.h"
using namespace std;

int evaluarJugada(const Carta seleccionadas[], int cantidad, bool esPrimeraMano, string& nombreJugada);
